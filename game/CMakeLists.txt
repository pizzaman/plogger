AUX_SOURCE_DIRECTORY(. GAME_SRCS)

INCLUDE_DIRECTORIES(
        ${PROJECT_SOURCE_DIR}/plogger
)

LINK_DIRECTORIES(
        ${PROJECT_BINARY_DIR}/libs
)

FILE(GLOB_RECURSE HEADER_FILES *.h)

ADD_EXECUTABLE(game ${GAME_SRCS} ${HEADER_FILES})

TARGET_LINK_LIBRARIES(
        game
        plogger
)
